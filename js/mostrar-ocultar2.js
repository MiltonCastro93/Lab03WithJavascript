function mostrarOcultar(){
    //Si la caja se ve
    if(caja.style.display == 'block'){
        //ocultamos la caja
        ocultar();
    }else{
        //mostramos la caja
        mostrar();
    }
}